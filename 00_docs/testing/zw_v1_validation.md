# ZW V1.0 本次验证记录

基线 `d4bf2f3`，验证日期 2026-09-17；使用 Windows 本机安装的 ESP-IDF `v6.1`、RISC-V GCC 15.2.0、esp_codec_dev 1.5.3。

| 验证 | 结果 | 范围 |
| --- | --- | --- |
| 默认保护配置完整编译/链接/生成 bin | 通过 | 硬件检查开关关闭；不会初始化负载 |
| 硬件开关 + 音频 + 深睡全开启配置完整编译 | 通过 | 仅编译，未在板上执行；不交付此配置的 bin |
| 扩展输出静态验证 | 通过 | 256 个旧锁存值 × 6 个输出 × 2 个使能状态；保持 P5 输入、P0 为低、其他输出不串改 |
| standby 位图检查 | 通过 | 0xAE；振动、雷达、舵机、充电关闭 |
| GXHT40 解码静态验证 | 通过 | CRC(0xBEEF)=0x92、量程端点、25°C 样本、湿度限幅 |
| GXHT40 单比特损坏检查 | 通过 | 六个字节中任意一位翻转，共 48 种，都被 CRC 拒绝 |
| PCB 网络归属提取 | 通过 | 503 个焊盘，与 Pads6 Header 数量一致；MCU/扩展器关键连接与 PDF 交叉检查 |
| Python 提取工具语法检查 | 通过 | py_compile |
| Git 补丁空白检查 | 通过 | git diff --check |

这些结果不等于 I²C 实测、音频质量验证、GPIO 电气验收、低功耗电流测量、充电安全验证或 PCB 铜皮 DRC。本次没有连接/烧录设备，没有修改 PCB 或 BOM，没有推送远程仓库。

构建环境问题及处理：

- Windows 深层路径使 Mbed TLS 的依赖文件路径过长；使用较短的独立构建目录后解决。subst 被 IDF 解析回真实路径，单独映射盘符不够。
- esp_codec_dev 1.5.3 的 SPI 源文件缺少版本宏头文件；通过项目 CMake 对该组件定向预包含 `esp_idf_version.h`，没有改动 managed_components。
- PowerShell `-D` 参数整体引用，避免 `sdkconfig.zw_v1` 被拆开。

重新验证静态检查可使用已安装的 C++17 编译器：

```text
riscv32-esp-elf-g++ -std=c++17 -Wall -Wextra -Werror -fsyntax-only -I. 08_tests/unit/zw_v1_board_test.cpp
```

同一静态检查文件也被编入 `zw_v1` 目标，无需在真实板上运行测试代码。

## 追加的全仓检查与修复

- 检查了行为/生命周期、OTA、通信、增长调度、灯光仲裁、振动和新板电源/中断路径。
- 修复普通行为立即语义完成后，Idle 状态下 StopBehavior 不停止残留灯光/振动的问题；停止时尝试全部输出，返回第一个错误，并将故障传递至生命周期，不重新启动警告振动。
- 新增服务级和 V2 命令入口回归测试。服务级测试在修改前出现 9 个失败检查，修复后全部通过；命令入口验证 Idle 停止及 HapticFailure 故障传播。
- 修复 BLE 断线/主机复位后残留接收命令：清队列，并为帧增加连接代次；出队时复查连接、加密、绑定和代次。已经交给业务层执行的命令不由接收队列撤销。BLE 重连的无线实测仍待开发板验证。
- 将 ZW 引脚/CRC 静态检查纳入主机 CMake 测试构建。
- WSL Ubuntu 22.04 / GCC 11.4：全量主机测试通过（CTest 的一个可执行文件包含全部单元及集成测试）。
- ESP-IDF 6.1：旧 V2 固件完整构建通过，ZW 默认保护配置构建通过。V2 构建只验证兼容性，不适用于此 ZW PCB。
- 未进行烧录、真实 BLE 重连、实际电流或舵机标定测试；不能据此认定产品已通过量产验收。

## 第二轮：脱离硬件的故障注入与协议压力测试

- 两个新增回归测试在修复前共触发 6 个失败检查，修复后通过：浅睡眠切换深睡眠失败后保留原模式并可正常唤醒；OTA 确认遇到时间回退后重新等待完整稳定区间。
- 旧板 ESP 电源适配层在深睡唤醒配置/定时器配置失败后尝试恢复 GPIO 浅睡唤醒源；此 SDK 分支经过编译检查，恢复失败和实际唤醒仍需硬件验证。
- 新增协议独立压力测试：2 个版本 × 256 个命令号 × 501 个载荷长度，共 256,512 种有效 CRC 帧形状，断言命令接受范围；每帧再破坏 CRC 并确认拒绝。另以固定种子生成 50,000 个 0～528 字节随机输入，使用精确尺寸分配以辅助发现越界读取。这是可重复的有限输入检查，不是协议全状态空间证明。
- GCC 11.4、Debug、AddressSanitizer + UndefinedBehaviorSanitizer、泄漏检测及遇错退出：CTest 2/2 通过，未发现检测器错误。检测范围为主机业务代码和协议；不包含 FreeRTOS 调度、NimBLE 无线栈、I2C 实时电气行为。
- 增加 PLANT_SANITIZERS CMake 开关及 GitHub Actions 工作流。远程 CI 结果以 GitHub Actions 为准，本地等价命令已通过。
- 这轮代码修复主要作用于旧 V2 的公共服务；ZW 仍为独立保护联调目标，未据此宣称业务已迁移或板级低功耗已达标。
